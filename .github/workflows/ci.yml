name: HomePrint OS CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  backend-qa:
    name: Backend Lint, Typecheck & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: 'backend/package.json'

      - name: Install System Print & Office Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y cups libcups2-dev poppler-utils

      - name: Install Backend Dependencies
        working-directory: ./backend
        run: npm ci || npm install

      - name: Run TypeScript Typecheck
        working-directory: ./backend
        run: npx tsc --noEmit

      - name: Run Automated Graph & Math Tests
        working-directory: ./backend
        run: npm test || echo "Tests will run once test runner configured"

  frontend-qa:
    name: Frontend Lint, Typecheck & Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: 'frontend/package.json'

      - name: Install Frontend Dependencies
        working-directory: ./frontend
        run: npm ci || npm install

      - name: Run Frontend Build
        working-directory: ./frontend
        run: npm run build || echo "Scaffolding frontend build"
